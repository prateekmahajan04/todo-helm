apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-todo-get-task
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-todo-get-task
  ports:
    - port: {{ .Values.getTask.ServicePort }}
      targetPort: {{ .Values.getTask.port }}
