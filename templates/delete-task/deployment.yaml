apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-todo-delete-tasks
spec:
  replicas: {{ .Values.deleteTask.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-todo-delete-tasks
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-todo-delete-tasks
    spec:
      containers:
      - name: {{ .Release.Name }}-todo-delete-tasks
        image: "{{ .Values.common.containerRegistryURL }}/{{ .Values.deleteTask.image.name }}:{{ .Values.deleteTask.image.tag }}"
        ports:
        - containerPort: {{ .Values.deleteTask.port }}
        env: 
        - name: CONNECTION_STRING
          value: "{{ .Values.deleteTask.connectionString }}"
